<phone:PhoneApplicationPage 
    x:Class="Smeedee.WP7.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Converters="clr-namespace:Smeedee.WP7.Converters" 
    xmlns:WP7="clr-namespace:Smeedee.WP7" mc:Ignorable="d" d:DataContext="{d:DesignData Source=SampleData/MainViewModelSampleData.xaml}"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="PortraitOrLandscape"  Orientation="Portrait"
    shell:SystemTray.IsVisible="True" d:DesignHeight="768" d:DesignWidth="480">
    
    <UserControl.Resources>
        <Converters:UriToImageConverter x:Key="UriToImageConverter"/>
    </UserControl.Resources>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <controls:Pivot Title="SMEEDEE">
            
            <!-- home screen -->
            <controls:PivotItem>
                <controls:PivotItem.Header>
                    <TextBlock Text="home" Foreground="#f25000"/>
                </controls:PivotItem.Header>
                <StackPanel Name="Home" Margin="12,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Image Source="Icon.png" Width="100"></Image>
                    <TextBlock Text="smeedee mobile" Style="{StaticResource PhoneTextLargeStyle}"></TextBlock>
                </StackPanel>                
            </controls:PivotItem>
            
            <!-- top committers -->
            <controls:PivotItem>
                <controls:PivotItem.Header>
                    <TextBlock Text="top committers" Foreground="#f25000"/>
                </controls:PivotItem.Header>
                <ListBox Margin="0,0,-12,0" ItemsSource="{Binding TopCommitters.Items}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,12,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ShowGridLines="False">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Image Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Margin="0,12,0,0" VerticalAlignment="Top" Source="{Binding Image, Converter={StaticResource UriToImageConverter}}"></Image>
                                <TextBlock Text="{Binding Name}" Grid.Column="1" Grid.Row="0" Style="{StaticResource PhoneTextLargeStyle}"/>
                                
                                <Grid Grid.Column="1" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <ProgressBar Grid.Column="0" Height="40" Width="Auto" HorizontalAlignment="Stretch" Background="#FFF25000" Foreground="#FFF25000" Minimum="0" Maximum="100" Value="{Binding CommitPercent}"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Commits}" Style="{StaticResource PhoneTextSubtleStyle}"/>
                                </Grid>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </controls:PivotItem>
            
            <!-- build status -->
            <controls:PivotItem>
            <controls:PivotItem.Header>
                <TextBlock Text="build status" Foreground="#f25000"/>
            </controls:PivotItem.Header>
                <ListBox Margin="0,0,-12,0" ItemsSource="{Binding BuildStatus.Items}">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,12,0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ShowGridLines="False">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding ProjectName}" Grid.Column="0" Grid.Row="0" Padding="0,12,0,0" Margin="12,0,0,0" Style="{StaticResource PhoneTextLargeStyle}"/>
                                <Image Source="{Binding BuildStatusImage}" Grid.Column="1" Grid.Row="0" HorizontalAlignment="Right" Width="50" Height="50"></Image>
                                <Grid Grid.ColumnSpan="2" Grid.Column="0" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ShowGridLines="False">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                            <TextBlock Text="{Binding UserName}" Grid.Column="0" Margin="12,0,0,0" Style="{StaticResource PhoneTextSubtleStyle}"/>
                            <TextBlock Text="{Binding BuildTime}" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="12,6,0,0" Style="{StaticResource PhoneTextSubtleStyle}" FontStyle="Italic" />
                            </Grid>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </controls:PivotItem>

			<!-- working days left -->            
            <controls:PivotItem>
                <controls:PivotItem.Header>
                    <TextBlock Text="days left" Foreground="#f25000"/>
                </controls:PivotItem.Header>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="12,0,0,0">
                    <TextBlock Name="WorkingDaysLeft_Days" Text="{Binding WorkingDaysLeft.DaysLeft}" HorizontalAlignment="Center" Foreground="#f25000" Style="{StaticResource PhoneTextTitle1Style}"></TextBlock>
                    <TextBlock Text="{Binding WorkingDaysLeft.DaysLeftSuffix}" TextWrapping="Wrap" HorizontalAlignment="Center" Style="{StaticResource PhoneTextLargeStyle}"></TextBlock>
                    <TextBlock Text="{Binding WorkingDaysLeft.UntillText}" TextWrapping="Wrap" HorizontalAlignment="Center" Style="{StaticResource PhoneTextLargeStyle}"></TextBlock>

                </StackPanel>
            </controls:PivotItem>

            <!-- latest commits -->
            <controls:PivotItem>
                <controls:PivotItem.Header>
                    <TextBlock Text="latest commits" Foreground="#f25000"/>
                </controls:PivotItem.Header>
                <WP7:LoadMoreListBox x:Name="LatestCommitsListBox2" Margin="0,0,-12,0" ItemsSource="{Binding LatestCommits.Items}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ShowGridLines="False">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <Image Grid.Row="0" Grid.RowSpan="3" Grid.Column="0" Source="{Binding Image, Converter={StaticResource UriToImageConverter}}" Width="55" VerticalAlignment="Top" Margin="0,19,0,7"></Image>

                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding User}" TextWrapping="Wrap" Style="{StaticResource PhoneTextLargeStyle}"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Message}" TextWrapping="Wrap" Margin="12,-6,12,0" Style="{StaticResource PhoneTextSubtleStyle}"/>
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Date}" TextWrapping="Wrap" Margin="12,-6,12,0" Style="{StaticResource PhoneTextSubtleStyle}" HorizontalAlignment="Right" FontStyle="Italic"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </WP7:LoadMoreListBox>
            </controls:PivotItem>
        </controls:Pivot>
    </Grid>
 
    <!--Sample code showing usage of ApplicationBar-->
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="Resources/Images/appbar.favs.rest.png" Text="View widgets"/>
            <shell:ApplicationBarIconButton IconUri="Resources/Images/appbar.feature.settings.rest.png" Text="Settings"/>
            <shell:ApplicationBarIconButton IconUri="Resources/Images/appbar.refresh.rest.png" Text="Refresh"/>
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="MenuItem 1"/>
                <shell:ApplicationBarMenuItem Text="MenuItem 2"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>