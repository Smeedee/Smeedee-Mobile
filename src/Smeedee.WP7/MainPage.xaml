<phone:PhoneApplicationPage 
    x:Class="Smeedee.WP7.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768" 
    d:DataContext="{d:DesignData Source=SampleData/MainViewModelSampleData.xaml}"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="PortraitOrLandscape"  Orientation="Portrait"
    shell:SystemTray.IsVisible="True">
    
    <UserControl.Resources>
        <Converters:UriToImageConverter x:Key="UriToImageConverter"/>
    </UserControl.Resources>

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <controls:Pivot Title="SMEEDEE">
            <!-- home screen -->
            <controls:PivotItem>
                <controls:PivotItem.Header>
                    <TextBlock Text="home" Foreground="#f25000"/>
                </controls:PivotItem.Header>
                <StackPanel Margin="12,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Image Source="Resources\Images\icon_smeedee.png" Width="100"></Image>
                    <TextBlock Text="smeedee mobile" Style="{StaticResource PhoneTextLargeStyle}" Margin="0,0,0,20"></TextBlock>
                </StackPanel>                
            </controls:PivotItem>
            
            <!-- top committers -->
            <controls:PivotItem>
                <controls:PivotItem.Header>
                    <TextBlock Text="top committers" Foreground="#f25000"/>
                </controls:PivotItem.Header>
                
                <ListBox Margin="0,0,-12,0" ItemsSource="{Binding TopCommitters.Items}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="12,0,0,0">
                                <Image Source="Resources\Images\default_person.jpeg"></Image>
                                <StackPanel Margin="0,0,0,17">
                                    <TextBlock Text="{Binding Name}" TextWrapping="Wrap" Style="{StaticResource PhoneTextLargeStyle}"/>
                                    <TextBlock Text="{Binding Commits}" TextWrapping="Wrap" Margin="12,-6,12,0" Style="{StaticResource PhoneTextSubtleStyle}"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </controls:PivotItem>
            <!-- build status -->
            <controls:PivotItem>
                <controls:PivotItem.Header>
                    <TextBlock Text="build status" Foreground="#f25000"/>
                </controls:PivotItem.Header>
                    <ListBox Margin="0,0,-12,0" ItemsSource="{Binding BuildStatus.Items}">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"></Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                
                                <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ShowGridLines="False">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="{Binding ProjectName}" Grid.Column="0" Grid.Row="0" Padding="0,12,0,0" TextWrapping="NoWrap" Margin="12,0,0,0" Style="{StaticResource PhoneTextLargeStyle}"/>
                                <TextBlock Text="{Binding UserName}" Grid.Column="0" Grid.Row="1" TextWrapping="NoWrap" Margin="12,0,0,0" Style="{StaticResource PhoneTextSubtleStyle}"/>
                                <TextBlock Text="{Binding BuildTime}" Grid.Column="0" Grid.Row="2" TextWrapping="NoWrap" Margin="12,6,0,0" Style="{StaticResource PhoneTextSubtleStyle}" FontStyle="Italic" />

                                <Image Source="Resources\Images\default_person.jpeg" Grid.Column="1" Grid.Row="0" Grid.RowSpan="3" HorizontalAlignment="Right" Width="100" Height="100"></Image>
                                
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
            </controls:PivotItem>

			<!-- working days left -->            
            <controls:PivotItem>
                <controls:PivotItem.Header>
                    <TextBlock Text="days left" Foreground="#f25000"/>
                </controls:PivotItem.Header>
                <StackPanel Orientation="Horizontal" Margin="12,0,0,0">
                    <TextBlock Text="{Binding WorkingDaysLeft.DaysLeft}" Style="{StaticResource PhoneTextExtraLargeStyle}"></TextBlock>
                    <TextBlock Text="{Binding WorkingDaysLeft.DaysLeftSuffix}" Style="{StaticResource PhoneTextLargeStyle}" Margin="0,0,0,20"></TextBlock>
                </StackPanel>
            </controls:PivotItem>


            <controls:PivotItem>
 			    <!-- latest commits -->     
                <controls:PivotItem.Header>
                    <TextBlock Text="latest commits" Foreground="#f25000"/>
                </controls:PivotItem.Header>
                <ListBox Margin="0,0,-12,0" ItemsSource="{Binding LatestCommits.Items}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="12,0,0,0" HorizontalAlignment="Stretch">
                                <Image Source="{Binding Image, Converter={StaticResource UriToImageConverter}}"></Image>

                                <StackPanel Margin="0,0,0,17" HorizontalAlignment="Stretch">
                                    <TextBlock Text="{Binding User}" TextWrapping="Wrap" Style="{StaticResource PhoneTextLargeStyle}"/>
                                    <TextBlock Text="{Binding Message}" TextWrapping="Wrap" Margin="12,-6,12,0" Style="{StaticResource PhoneTextSubtleStyle}"/>
                                    <TextBlock Text="{Binding Date}" TextWrapping="Wrap" Margin="12,-6,12,0" Style="{StaticResource PhoneTextSubtleStyle}"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </controls:PivotItem>
        </controls:Pivot>
    </Grid>
 
    <!--Sample code showing usage of ApplicationBar-->
    <!--<phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button1.png" Text="Button 1"/>
            <shell:ApplicationBarIconButton IconUri="/Images/appbar_button2.png" Text="Button 2"/>
            <shell:ApplicationBar.MenuItems>
                <shell:ApplicationBarMenuItem Text="MenuItem 1"/>
                <shell:ApplicationBarMenuItem Text="MenuItem 2"/>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>-->

</phone:PhoneApplicationPage>